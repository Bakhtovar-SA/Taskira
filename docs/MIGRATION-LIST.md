<!-- GENERATED by scripts/generate-docs.ts from server/migrations/*.sql — DO NOT EDIT. -->
# Список миграций

Порождается скриптом (`npm run docs:generate`), руками не редактируется. Правила написания миграций — в
[MIGRATIONS.md](./MIGRATIONS.md): исторические `001`–`029` неизменяемы, новые файлы — с префиксом-меткой времени.

## Исторические (последовательные)

| Файл | Первая строка описания |
|---|---|
| `001_init.sql` | Миграция 001: базовая схема PostgreSQL. Применение: psql "$DATABASE_URL" -f 001_init.sql Данные (проект, статусы workflow, первый админ) создаёт seed-скрипт сервера (Этап 3): npm run seed --prefix server |
| `002_corporate.sql` | Миграция 002: корпоративная модель (не Jira-клон). |
| `003_counters.sql` | Миграция 003: счётчики номеров задач. |
| `004_project_roles.sql` | Миграция 004: ролевая модель, привязанная к проекту. |
| `006_drop_access_role.sql` | Миграция 006: удаление легаси-колонки users.access_role. |
| `007_departments.sql` | Миграция 007: департаменты + подготовка к multi-project. |
| `008_issue_collaborators.sql` | Миграция 008: участники задачи (issue collaborators). |
| `009_ldap.sql` | Миграция 009: подготовка к LDAP/AD-аутентификации. |
| `010_attachments.sql` | Миграция 010: вложения к задачам (файлы). |
| `011_notifications.sql` | Миграция 011: уведомления (in-app + email). |
| `012_drop_sprints.sql` | Миграция 012: полное удаление спринтов. |
| `013_priority_four_levels.sql` | Миграция 013: приоритеты 5 значений → 4. |
| `014_issue_links.sql` | Миграция 014: связи между задачами (relates / blocks). |
| `015_notification_dismiss.sql` | Миграция 015: скрытие уведомлений из собственной ленты. |
| `016_issue_lifecycle.sql` | 016: жизненный цикл задачи — дата закрытия и архив. |
| `017_token_revocation.sql` | 017: отзыв выданных токенов. |
| `018_issue_complexity.sql` | Миграция 018: points → complexity. |
| `019_checklist_items.sql` | Миграция 019: чек-листы на задаче. |
| `020_custom_fields.sql` | Миграция 020: пользовательские поля проекта. |
| `021_subtasks.sql` | Миграция 021: подзадачи (issues.parent_id). |
| `022_issue_templates.sql` | Миграция 022: шаблоны задач. |
| `023_sprints.sql` | Миграция 023: спринты — опциональный, отключаемый по умолчанию модуль (SPRINTS_MIGRATION.md). |
| `024_favorite_projects.sql` | Миграция 024: избранные проекты пользователя. |
| `025_multiple_assignees.sql` | Миграция 025: несколько исполнителей на задаче. |
| `026_ad_profile_attrs.sql` | Миграция 026: телефон + AD-атрибуты профиля. |
| `027_user_avatars.sql` | Миграция 027: аватарки пользователей (самообслуживание). |
| `028_notify_email_mandatory.sql` | Миграция 028: почтовые уведомления больше нельзя выключить. |
| `029_session_version.sql` | Монотонная версия сессии. В отличие от сравнения часов приложения с PostgreSQL не зависит от clock skew между хостами. |

## С меткой времени

| Файл | Первая строка описания |
|---|---|
| `20260919T1500_security_hardening.sql` | (нет комментария-описания) |
| `20260920T0631_index_active_issues_page.sql` | CREATE INDEX CONCURRENTLY нельзя выполнять внутри BEGIN. При обрыве PostgreSQL может оставить indisvalid=false; runner не отметит миграцию применённой, а команда recovery выше безопасно удаляет частичный объект перед повтором. |
| `20260920T1400_index_issues_sort_priority.sql` | Сортировка «Списка задач» (priority) по активным задачам проекта. Выражение и порядок колонок совпадают с SORT_EXPR в services/issueFilters.ts; num — тай-брейк (уникален в проекте), индекс читается в обе стороны. Одна команда на файл: CREATE INDEX CONCURRENTLY нельзя запускать в составе нескольки… |
| `20260920T1401_index_issues_sort_due.sql` | Сортировка «Списка задач» (due) по активным задачам проекта. Выражение и порядок колонок совпадают с SORT_EXPR в services/issueFilters.ts; num — тай-брейк (уникален в проекте), индекс читается в обе стороны. Одна команда на файл: CREATE INDEX CONCURRENTLY нельзя запускать в составе нескольких ком… |
| `20260920T1402_index_issues_sort_updated.sql` | Сортировка «Списка задач» (updated) по активным задачам проекта. Выражение и порядок колонок совпадают с SORT_EXPR в services/issueFilters.ts; num — тай-брейк (уникален в проекте), индекс читается в обе стороны. Одна команда на файл: CREATE INDEX CONCURRENTLY нельзя запускать в составе нескольких… |
| `20260920T1410_issue_has_assignee.sql` | PERF-05: денормализованный признак «есть исполнитель» для фильтра assignee=none. Ленивый список «нераспределённых задач» считается anti-join'ом issues × issue_assignees (39–53 мс на 50 000 задач, индекса под него нет). Флаг поддерживается триггером на issue_assignees, а не кодом приложения: так о… |
| `20260920T1411_index_issues_unassigned.sql` | «Нераспределённые» активные задачи проекта: счётчик по статусам (index-only) и страницы в порядке rank с фильтром по статусу. Требует has_assignee (20260920T1410). |
| `20260920T1420_trigram_search_indexes.sql` | SEARCH-01: триграммные индексы для текстового поиска задач (фильтр `q`). `ILIKE '%…%'` по title/key не использует btree и на 50 000 задач читает всю таблицу (61–232 мс); GIN по pg_trgm — 1–6 мс. |
| `20260921T1000_login_attempts.sql` | RESTART-SAFETY: лимит попыток входа по IP переезжает из памяти процесса в БД. Счётчик в памяти (`Map` в routes/auth.ts) у каждого процесса свой: при втором экземпляре на той же БД эффективный лимит — «10 × число процессов», а перезапуск обнуляет его. Блокировка по АККАУНТУ (`users.failed_login_at… |
| `20260922T1100_saved_views.sql` | ТЗ 3.2 (план v2, Трек 3): сохранённые фильтры/вьюхи. Аддитивная миграция — новая таблица, ничего существующего не трогает. |
| `20260922T1400_fulltext_search.sql` | ТЗ 3.4 (план v2 Трек 3): полнотекстовый поиск по названию/описанию задачи, комментариям и пунктам чек-листа. Аддитивная миграция — только новые generated-колонки и индексы, ничего существующего не трогает. |
| `20260922T1500_instance_singleton.sql` | ТЗ 4.1 (план v2, Трек 4): singleton-таблица instance вместо будущей сущности organizations. Решение и его обоснование — docs/adr/0009-database-per-tenant.md: при database-per-tenant одна БД физически не может содержать вторую организацию, и схема обязана выражать это явно, а не полагаться на согл… |
