-- ============================================================
-- Taskira. Миграция 015: скрытие уведомлений из собственной ленты.
-- Применяется server/src/db.ts migrate() целиком в одной транзакции.
--
-- dismissed_at — мягкое скрытие, НЕ удаление строки: аудит-след и
-- email_state/email_tries остаются нетронутыми, воркер рассылки эту
-- колонку не читает и не учитывает. Пользователь скрывает только свои
-- уведомления (WHERE user_id = $1 в роуте) — на других не влияет.
--
-- NULL = видимо в ленте (как раньше). Обратима:
--   ALTER TABLE notifications DROP COLUMN dismissed_at;
-- ============================================================

ALTER TABLE notifications ADD COLUMN dismissed_at timestamptz;

-- Лента получателя уже фильтрует по user_id + created_at (idx_notifications_user);
-- dismissed добавляется как доп. условие в WHERE самого запроса (низкая
-- кардинальность/редкое использование — отдельный индекс пока не нужен).
