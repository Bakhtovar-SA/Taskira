-- ============================================================
-- Taskira. Миграция 026: телефон + AD-атрибуты профиля.
-- Применяется server/src/db.ts migrate() целиком в одной транзакции.
--
-- users.job_role уже существует (001_init.sql) и означает должность в
-- компании, а не роль доступа — этого поля достаточно для AD-атрибута
-- title, отдельная колонка не нужна. Телефона в схеме не было вообще —
-- добавляем users.phone тем же соглашением not-null-default-empty, что
-- у job_role, а не nullable: избавляет весь остальной код от null-проверок
-- там, где для job_role их сегодня тоже нет.
--
-- LDAP-синк (services/userProvisioning.ts, следующим коммитом) начинает
-- писать job_role/phone из AD-атрибутов title/telephoneNumber при каждом
-- логине LDAP-пользователя. Это сознательно отменяет для этих двух полей
-- решение LDAP_MIGRATION.md D4 "job_role переживает LDAP-adopt без
-- изменений" — по запросу: то самое поле "Должность" должно показывать
-- актуальную должность из AD, а не то, что было вбито вручную при
-- заведении локального аккаунта до перехода на LDAP. Для локальных
-- пользователей (auth_source='local') ничего не меняется — синк их
-- никогда не трогает.
-- ============================================================

ALTER TABLE users ADD COLUMN phone text NOT NULL DEFAULT '';
