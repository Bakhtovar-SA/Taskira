# UI_RESTRUCTURE — функциональная реструктуризация интерфейса

Статус: **все фазы реализованы.** Фаза 0 — PR #20 (влит в `main`). Фазы 1–6 —
ветка `feat/ui-restructure`, один PR: Ф1 (миграция 012, спринты удалены),
Ф5 («Список задач»), Ф2 («Направление»), Ф3 («+» у первого `todo`-столбца),
Ф4 (главный экран + `GET /api/issues/assigned-to-me`), Ф6 (чек-лист в
`server/README.md`, `ARCHITECTURE.md` п. 6, `SCOPE.md`).
Проверка: `npm test` (сервер) 78 зелёных / 20 skipped; `typecheck` 0 (обе
стороны); `npm run build`; живые прогоны «Списка задач», доски и главного
экрана через `scripts/shot.mjs`.
Порядок фаз был: 0 → 1 → 2 → 3 → 4 → 5 → 6.

**Только функциональная часть.** Цвета, отступы, типографику, иконки — не трогаем;
новые экраны (главный, «Список задач») собираются из существующих компонентов
(`Empty`, `Chip`, `Avatar`, `Dropdown`) и уже принятых utility-классов, без нового
визуального языка. Визуальный редизайн — отдельным заходом с пользователем позже.

Контекст: [SCOPE.md](SCOPE.md) — «Спринты, scrum-церемонии, оценка в story points»
в разделе «Осознанно откладываем / не делаем»; «Эпик → Направление», «Backlog →
Список задач» в таблице терминологии. [ARCHITECTURE.md](ARCHITECTURE.md) — п. 6
«Порядка разработки» (нейтральная терминология UI) и «Что убирается / упрощается»
(`Backlog.tsx` без спринтов, story points → «сложность»). Предыдущие миграции —
[ROLE_MIGRATION.md](ROLE_MIGRATION.md), [DEPT_MIGRATION.md](DEPT_MIGRATION.md),
[COLLAB_MIGRATION.md](COLLAB_MIGRATION.md), [LDAP_MIGRATION.md](LDAP_MIGRATION.md),
[FILES_MIGRATION.md](FILES_MIGRATION.md),
[NOTIFICATIONS_MIGRATION.md](NOTIFICATIONS_MIGRATION.md).

Также закрывает открытые пункты финального аудита
`taskira-review-3-final.md` (см. §5).

---

## 1. Зачем

Интерфейс несёт наследие ранней Jira-подобной модели, от которой
[SCOPE.md](SCOPE.md) осознанно отказался: спринты и scrum-церемонии, «эпик» вместо
«направления», вход сразу в один проект (нелогично при нескольких отделах),
«Бэклог» как экран планирования спринтов. Плюс мелкие точки трения (кнопка «+» в
каждой колонке доски). Задача — привести UI к модели SCOPE **до** визуального
редизайна, чтобы дизайн делался уже по правильной структуре.

Пять изменений (из задания):

1. Убрать спринты полностью — таблица `sprints`, `issues.sprint_id`, роуты, право
   `manageSprints`, весь клиентский UI.
2. «Эпик» → «Направление» — везде в подписях и текстах. Поле `epicId` (и вся
   серверная модель группировки) **не меняется** — только отображение.
3. Создание задачи — только через кнопку «+ Создать» в шапке и «+» в первой
   колонке доски («К выполнению»); из остальных колонок «+» убрать.
4. Главный экран при входе (до выбора проекта): «Мои задачи» (назначенные мне по
   всем доступным проектам) + «Недавние проекты» (доступные отделы/проекты).
5. «Бэклог» → «Список задач»: плоский список задач с фильтрами, без секций
   спринтов.

---

## 2. Что в коде сейчас

### Спринты

| Слой | Файл | Что есть |
|---|---|---|
| Схема | `001_init.sql` | таблица `sprints` (`status future\|active\|completed`, `start_date`/`end_date`), `issues.sprint_id uuid REFERENCES sprints ON DELETE SET NULL`, индекс `idx_issues_sprint` |
| Сид | `seedProject.ts` | вставляет один `future`-спринт в каждый новый проект |
| Сервис | `services/sprints.ts` | `SprintRow`/`SprintDto`/`mapSprint` |
| Роуты | `routes/sprints.ts` | `GET /sprints`, `POST /sprints/start`, `POST /sprints/:id/complete` (все `requirePerm(...)`) |
| Роут задач | `routes/issues.ts` | `PATCH /:id` — ветка смены `sprintId` под инлайн-проверкой `manageSprints`; отдельный `PATCH /:id/sprint`; `IssueQuery.sprint` фильтр; `INSERT` задачи с `sprint_id` |
| DTO | `services/issues.ts` | `IssueRow.sprint_id` / `IssueDto.sprintId` в `mapIssue` |
| Bootstrap | `routes/projects.ts` | `GET /projects/:id` отдаёт `sprints: SprintDto[]` |
| Контракт | `contract.ts` | `SPRINT_STATUSES`, `MoveToSprintBody`, `IssueCreateBody.sprintId`, `IssuePatchBody.sprintId`, `IssueQuery.sprint`, `WsMessage` вариант `sprint:changed` |
| Права | `permissions.ts` ×2 | `PermId` `manageSprints`; `MATRIX.manageSprints = ["admin","manager"]`; `PERM_NAMES`/`PERMISSIONS` записи |
| Клиент — api | `src/api/index.ts` | `sprintsApi.{start,complete}`; `issuesApi.setSprint` |
| Клиент — типы | `src/types.ts` | `Sprint`, `Data.sprints`, `Issue.sprintId`; `ViewId` без отдельного пункта |
| Клиент — store | `src/store.tsx` | `data.sprints`, экшены `setSprint` / `startSprint` / `completeSprint`, `mapIssue.sprintId` |
| Клиент — UI | `Backlog.tsx` | **весь экран** построен на спринтах: 3 секции (активный / будущий / вне спринтов), drag между секциями, кнопки «Начать/Завершить спринт» |
| | `Board.tsx` | фильтрует задачи по активному спринту; баннер «спринт не активен — показаны все задачи», «осталось N дн» |
| | `IssueModal.tsx` | поле «Спринт» (под `can("manageSprints")` — уже поправлено) |
| | `CreateIssueModal.tsx` | селект «Спринт» (дефолт — активный) |
| | `DocsView.tsx` | упоминания спринтов в текстах и «модели данных» |
| Аудит | `audit_log` | строки `entity='sprint'`, `action` `sprint.start`/`sprint.complete`/`issue.sprint.move` — исторические, FK нет |

### Эпик

- Тип `epic` **уже упразднён** (миграция 002: `type_id IN ('task','bug','request')`).
  «Эпик» сейчас = обычная задача, на которую ссылаются через `issues.epic_id`.
- Отображение: `IssueModal` поле «Эпик», `CreateIssueModal` селект «Эпик»,
  `Board.tsx`/`Backlog.tsx` бейдж эпика на карточке, `TimelineView` («Дорожная
  карта эпиков», «Эпиков пока нет», колонка «Эпик»), `DocsView` тексты.
- `Issue.epicId`, `IssueCreateBody.epicId`, `getIssueDto.epicId`,
  `issues.epic_id`, `issues.color` («цвет эпика»), `issues.t_start`/`t_span`
  (таймлайн) — **менять не нужно** (D2).

### Вход / выбор проекта

- `src/store.tsx` `bootstrap()`: `authApi.me()` → `projectsApi.list()` →
  `chosen = readLastProject() ?? projects[0]` → `buildProjectData(chosen)` →
  `bootStatus='ready'`. **Экрана выбора нет** — сразу в проект.
- `switchProject(id)` перегружает `Data` под новый проект.
- 0 проектов → либо `solo` (есть приглашения), либо пустой экран.
- Топбар: хлебная крошка «Проекты › <проект> › <вид>» (не кликабельна как
  «домой»).

### Доска — создание задачи

- `Board.tsx`: в шапке каждой колонки при `canCreate` — кнопка «+»
  (`setQuickFor(st.id)` → инлайн `<QuickCreate>`).
- Топбар: кнопка «+ Создать» → `setCreateOpen(true)` → `<CreateIssueModal>`.

### «Бэклог»

- `Sidebar.tsx`: пункт `{ id: "backlog", label: "Бэклог", kbd: "2" }`.
- `Backlog.tsx`: нет фильтров вообще — только секции спринтов и drag.
- Фильтры задач **на сервере есть** (`IssueQuery`: `status`, `assignee`, `type`,
  `q`, `dueFrom/dueTo`, `overdue`), `Board.tsx` использует свой клиентский фильтр.

### Финальный аудит (`taskira-review-3-final.md`) — открытые пункты

| # | Находка | Актуальность |
|---|---|---|
| §2.1 | Поле «Спринт» в `IssueModal` под `editOk`, а не `manageSprints` | **Снимается Фазой 1** (поле удаляется). В текущем коде `IssueModal.tsx:591` уже `can("manageSprints")` — т.е. по факту закрыто, но проверить. |
| §2.2 | `label()` в `contract.ts` без `.min(1)` — можно создать пустую метку | **Актуально.** Фаза 0. |
| §3.1 | Неиспользуемые корневые зависимости (`@dnd-kit/*`, `recharts`, `framer-motion`, `react-router-dom`, `uuid`, `date-fns`, …) | **Актуально.** Фаза 0. Проверить `uuid`/`date-fns` заново перед удалением. |
| §3.2 | `package.json` (корень) `name: "sandbox-workspace"` | **Актуально.** Фаза 0. |
| §3.3 | Клиентский `typecheck`/`build` не в CI (job'ы только `working-directory: server`) | **Актуально и важно** — сделать до активной правки клиента. Фаза 0. |
| §1.1 | `ldapjs` (devDep) архивирован автором | **Не в этом заходе** (решение D6): нулевой риск, но не по теме UI — отдельный тикет. |

---

## 3. Ключевые решения (РЕШЕНО)

Подтверждено, как предложено (уточнения по D4/D5 внесены после ревью):

| # | Решение |
|---|---|
| **D1** спринты | Полное удаление, **не** tombstone. Миграция `012_drop_sprints.sql` (`DROP COLUMN issues.sprint_id`; `DROP TABLE sprints`) — необратима. Дамп `sprints` **не делаем** (на dev-БД там только дефолтный seed-спринт на проект, реального планирования нет). Право `manageSprints` удаляется из `MATRIX` в обеих копиях `permissions.ts` — это снятие возможности, а не переназначение прав (замороженной матрицы [ROLE_MIGRATION §3.2](ROLE_MIGRATION.md) не касается). `audit_log`-строки про спринты не трогаем. |
| **D2** «направление» | Только отображаемый термин («Эпик» → «Направление» в подписях, текстах, `TimelineView`, `DocsView`). `Issue.epicId` / `epic_id` / `IssueCreateBody.epicId` / `getIssueDto.epicId` / `issues.color`/`t_start`/`t_span` — **без изменений** (переименование поля в контракте API — ломающее изменение ради косметики, отклонено). |
| **D3** создание задачи | «+ Создать» в шапке — без изменений. Инлайн-«+» (`<QuickCreate>`) на доске оставляем **только у первого столбца с `category === 'todo'`** (по `position`); у остальных убираем. Правило «первый по позиции» однозначно и для кастомного workflow. |
| **D4** главный экран | Новый `GET /api/issues/assigned-to-me` (project-less, `requireAuth`) + `<HomeView>` («Мои задачи» + «Недавние проекты»). Показывается при **≥ 2** доступных проектах; при 1 — сразу в проект (как сейчас); при 0 — solo / пустой экран. Переход между «≥2» и «1» описан явно ниже (D4-transition). Возврат на главный из проекта — кликабельная крошка «Проекты» в топбаре. |
| **D5** «Список задач» | `Backlog.tsx` → плоский список всех `data.issues` + панель **фильтров И сортировки** (обе — клиентские, задачи уже загружены, лимит 200). Сортировку добавляем **сразу**, не в follow-up. Секции спринтов, drag между секциями, кнопки «Начать/Завершить спринт», спринт-пункты контекстного меню — удалить. `ViewId` id `backlog` и `kbd:"2"` остаются внутренними (переименование id — отдельно). |
| **D6** гигиена | Фаза 0, **отдельным PR первой**: `client`-job в `test.yml` (typecheck + build корня), `contract.ts` `label().min(1)`, `npm uninstall` неиспользуемых корневых зависимостей (после `grep`-проверки каждой), `package.json` `name` → `taskira`. **`ldapjs` НЕ трогаем** — гигиена нулевого риска, но тематически не связана с UI; отдельный тикет на будущее. |
| **D7** порядок | Фаза 0 — отдельный PR. Фазы 1–6 — одна ветка `feat/ui-restructure`, один PR. |
| **D8** вне захода | Визуальный редизайн; story points → «сложность»; переименование `epicId`/`ViewId`/kbd; полный `DocsView`; судьба `TimelineView`; серверная фильтрация «Списка задач»; `ldapjs`. |

### D4-transition — поведение при изменении числа доступных проектов *(осознанное, не побочный эффект)*

- **1 → ≥2** (пользователю открыли второй проект / проект стал `is_shared` / он
  попал во второй департамент). При следующем входе `bootstrap()` видит ≥2
  проекта → показывает `<HomeView>` вместо прямого входа. Чтобы это не выглядело
  внезапной сменой поведения: при **первом в жизни этого пользователя** показе
  `<HomeView>` — одноразовый `toast("info", …)` вроде «Вам открыт ещё один проект.
  Теперь при входе — список ваших проектов и задач; открыть проект напрямую можно
  здесь». Флаг «показано» — в `localStorage` (`taskira.homeIntro`), не на сервере.
  Тот же тост увидит и новый пользователь, которому сразу открыли ≥2 проекта —
  это нормально (объяснение экрана).
- **≥2 → 1** (пользователя убрали из проектов, остался один). При следующем
  входе — сразу в оставшийся проект (меньше выбора → главный экран не нужен).
  Пояснения не требуется; флаг `taskira.homeIntro` не сбрасываем (если проектов
  снова станет ≥2 — тост уже показан, повторять не нужно).
- **Прямая ссылка `#/issue/<pid>/<iid>`** при входе — ведёт в нужный проект
  (`switchProject` + открыть), минуя `<HomeView>`, независимо от числа проектов.

Ниже — обоснования и отклонённые альтернативы по каждому решению.

### D1. Спринты — полное удаление, включая право `manageSprints` и колонку `sprint_id`

**Рекомендация.**
- **Миграция `012_drop_sprints.sql`**: `ALTER TABLE issues DROP COLUMN sprint_id;`
  (снимает FK и `idx_issues_sprint`) → `DROP TABLE sprints;`. Бэкфилла нет.
  **Необратима** (данные спринтов и привязки задач теряются) — как `006` (drop
  `access_role`). Перед применением на БД с данными — снять дамп
  (`pg_dump -t sprints -a`), если планирование в спринтах кому-то важно для
  истории. На dev — только сид-спринт, потеря безболезненна.
- **Право `manageSprints` удаляется из `MATRIX` в обеих копиях `permissions.ts`**
  (не оставляем tombstone-ключ): `PermId`, `MATRIX`, `PERM_NAMES`,
  клиентский `PERMISSIONS`. Все вызовы (`roleHas(.., "manageSprints")`,
  `requirePerm("manageSprints")`, `can("manageSprints")`) снимаются в этой же
  фазе, одним коммитом.
- **Сервер:** удалить `routes/sprints.ts`, `services/sprints.ts`, регистрацию
  `sprintRoutes` в `app.ts`; из `routes/issues.ts` — ветку `sprintId` в
  `PATCH /:id`, весь `PATCH /:id/sprint`, `IssueQuery.sprint`, `sprint_id` в
  `INSERT` и `mapIssue`; из `routes/projects.ts` bootstrap — `sprints`; из
  `contract.ts` — `SPRINT_STATUSES`, `MoveToSprintBody`, `sprintId` в
  `IssueCreateBody`/`IssuePatchBody`, `sprint` в `IssueQuery`, вариант
  `sprint:changed` в `WsMessage`; из `services/issues.ts` — `sprint_id`/`sprintId`;
  из `seedProject.ts` — вставку спринта.
- **`audit_log`** — строки `entity='sprint'` и `action LIKE 'sprint%'` / `issue.sprint.move`
  **не трогаем** (исторический журнал, `entity`/`action` — свободный текст, FK нет).
- **Клиент:** удалить `Sprint` тип, `Data.sprints`, `Issue.sprintId`, `sprintsApi`,
  `issuesApi.setSprint`, экшены `setSprint`/`startSprint`/`completeSprint`,
  `sprintId` в `mapIssue`; из `Board.tsx` — фильтрацию по спринту и баннер
  (доска всегда показывает все задачи проекта); поле «Спринт» из `IssueModal` и
  `CreateIssueModal`. `Backlog.tsx` переписывается в Фазе 5.

**Обоснование.** SCOPE с самого начала помещает спринты в «не делаем»; держать
мёртвую таблицу, право и три роута — только запутывает. `manageSprints` не
относится к «замороженной матрице» в смысле [ROLE_MIGRATION.md §3.2](ROLE_MIGRATION.md)
(там про `editWorkflow`/`manageAccess`) — это удаление *возможности*, а не
переназначение прав; матрица меняется в обеих копиях одним коммитом, как
`manageCollaborators` добавлялся.

**Отклонено.**
- *Оставить `sprints` в БД, убрать только UI* — «зомби-схема», роуты и право
  остаются поверхностью; при следующем чтении кода снова вопрос «а это точно не
  используется».
- *Tombstone-ключ `manageSprints` в `MATRIX`* — компилируется, но `PermissionsView`
  показывает строку про несуществующую фичу; чистое удаление честнее.
- *Оставить `issues.sprint_id` «на всякий»* — nullable-колонка без смысла;
  `epic_id`/`color`/`t_start`/`t_span` остаются (D2), `sprint_id` — нет.

### D2. «Эпик» → «Направление» — только отображение

**Рекомендация.** Меняем строки в UI и текстах:
- `IssueModal.tsx`: `<Field label="Эпик">` → `«Направление»`; «Без эпика» → «Без
  направления».
- `CreateIssueModal.tsx`: подпись «Эпик» → «Направление», «Без эпика» → «Без
  направления».
- `Board.tsx` / `Backlog.tsx`: бейдж эпика — логика та же, подпись/`title` (если
  есть) → «направление».
- `TimelineView.tsx`: «Дорожная карта эпиков…» → «…направлений», «Эпиков пока
  нет» → «Направлений пока нет», подсказки, колонка «Эпик» → «Направление».
- `DocsView.tsx`: заменить «эпик»/«эпики» на «направление»/«направления» в
  тексте; заодно убрать абзацы про спринты (они устаревают Фазой 1). Полный
  переписывание `DocsView` (там ещё упоминания localStorage и «Сбросить
  демо-данные») — **вне захода**, отдельный пункт.
- Внутренние идентификаторы **не трогаем**: `Issue.epicId`, `epic_id`,
  `IssueCreateBody.epicId`, `getIssueDto.epicId`, `ViewId` `timeline`,
  комментарии в коде («тип epic упразднён…») можно оставить как есть.

**Обоснование.** SCOPE прямо: «Epic / эпик → Направление». Поле остаётся тем же —
меняется только слово, которое видит пользователь. Переименовывать `epicId` в
коде — лишний диф без пользы (и рискованно: поле в контракте API).

**Отклонено.** Переименование `epicId` → `directionId` в типах/контракте —
ломающее изменение API ради косметики; отложено (если вообще нужно).

### D3. Создание задачи — «+ Создать» в шапке + «+» только в первой колонке доски

**Рекомендация.** В `Board.tsx` кнопку «+» (инлайн `<QuickCreate>`) оставляем
**только у первого столбца с `category === 'todo'`** (по `position`); у остальных
столбцов кнопку убираем. Кнопка «+ Создать» в топбаре (`<CreateIssueModal>`) —
без изменений. `<QuickCreate>` создаёт задачу сразу в статусе своего столбца — для
первого `todo`-столбца это ожидаемо («К выполнению»).

**Обоснование.** Быстрое создание нужно только «в начало потока»; «+» в колонках
«В работе»/«На ревью»/«Готово» провоцирует создавать задачу сразу в неестественном
статусе. Общая кнопка в шапке остаётся для случаев «создать и заполнить поля».

**Отклонено.**
- *Убрать «+» из всех колонок, оставить только шапку* — теряется удобный
  quick-add в основном сценарии (накидать задач в «К выполнению»).
- *Оставить «+» во всех `todo`-столбцах* — дефолтный workflow имеет один `todo`,
  но кастомный может иметь несколько; правило «первый по позиции» однозначно.

### D4. Главный экран при входе — «Мои задачи» + «Недавние проекты»

**Рекомендация.**
- **Сервер:** новый `GET /api/issues/assigned-to-me` (project-less, на уровне
  `/api`, `requireAuth` — как `/api/issues/collaborating`): открытые задачи
  (`ws.category <> 'done'`), где `assignee_id = me`, по всем проектам, которые
  пользователь может `browse` (участник ∪ департамент ∪ `is_shared` ∪ глоб.
  admin — тот же предикат, что `listVisibleProjects`). Ответ:
  `{ issueId, projectId, key, title, statusName, statusCategory, priorityId,
  projectKey, projectName }`. Сортировка — по приоритету, затем `updated_at`.
- **Клиент:**
  - `bootStatus` += `"home"` (или переиспользуем `"ready"` + `ui.view="home"` без
    загруженного проекта — решить при реализации; чище отдельный статус).
  - `bootstrap()`: при **≥ 2 доступных проектах** — грузим список проектов +
    `assigned-to-me` + уведомления, ставим `bootStatus="home"`, **в проект не
    входим**. При **ровно 1 проекте** — как сейчас (сразу в него, главный экран
    бессмыслен). При 0 — как сейчас (solo / пустой экран).
  - `<HomeView>` *(новый)*: шапка (лого, колокол, меню пользователя — переиспользовать
    из топбара), два блока:
    - **«Мои задачи»** — список из `assigned-to-me`, строка = ключ · проект ·
      заголовок · статус · приоритет; клик → войти в проект и открыть задачу
      (`switchProject(projectId)` затем `#/issue/<pid>/<iid>` через существующий
      hash-механизм).
    - **«Недавние проекты»** — проекты, сгруппированные по департаменту
      (`data.departments` + `projects`), последний открытый (`readLastProject`) —
      первым, помечен «продолжить». Клик по проекту → `switchProject` → обычная
      оболочка.
  - Вернуться на главный экран из проекта: хлебная крошка «Проекты» в топбаре
    делается кликабельной → `bootStatus="home"` (проект выгружать не обязательно —
    просто показать `<HomeView>` поверх; уточнить).
- `writeLastProject` вызывается при входе в проект (уже так); главный экран его
  читает для «продолжить».

**Обоснование.** При нескольких отделах/проектах «кинуть сразу в один» —
дезориентирует. «Мои задачи по всем проектам» — самый частый вход («что мне
делать?»). Эндпоинт по образцу уже существующего `/issues/collaborating` —
project-less, дёшево.

**Отклонено.**
- *Всегда показывать главный экран, даже при 1 проекте* — лишний клик, когда
  выбора нет.
- *Считать «мои задачи» на клиенте из уже загруженного проекта* — покрывает
  только текущий проект, а смысл экрана — кросс-проектный обзор.
- *Полноценный дашборд с виджетами/графиками* — вне захода (и вне «без
  визуального редизайна»); MVP — два списка.

### D5. «Бэклог» → «Список задач» — плоский список с фильтрами

**Рекомендация.**
- `Sidebar.tsx`: пункт `backlog` → label «Список задач» (id `backlog` и `kbd:"2"`
  оставляем внутренними, чтобы не трогать `ViewId`/`App.tsx`/маршрутизацию хэша —
  минимальный диф; переименование id в `list` — опционально, отдельно).
- `Backlog.tsx` переписывается: секции спринтов, drag между секциями, кнопки
  «Начать/Завершить спринт» — **удалить**. Остаётся **плоский список** всех
  `data.issues` + панель **фильтров** и **сортировки** (обе клиентские):
  - фильтры: статус (по `data.workflow.statuses`), исполнитель (`data.users`),
    тип (`ISSUE_TYPES`), текст (по `title`/`key`), «просроченные»
    (`dueDate < сегодня` и статус не `done`);
  - сортировка (добавляем **сразу**, не follow-up): по приоритету
    (`PRIORITY_ORDER`, highest→lowest — дефолт), по сроку (ближайший первым, без
    срока — в конец), по обновлению (свежие первыми), по ключу. Направление —
    выбор + переключатель asc/desc.
  Строка списка: ключ · тип · заголовок · направление-бейдж · исполнитель ·
  статус · срок. Клик → `openIssue`.
- Контекстное меню строки: «Открыть», «Удалить» (при праве `delete`) — как
  сейчас; пункты «В спринт …» / «Вернуть в бэклог» — убрать.

**Обоснование.** SCOPE: «Backlog → Список задач», «плоский список задач с
фильтрами, без понятий "активный/будущий спринт"». Фильтры UX-only; серверные
`IssueQuery` уже есть, при желании позже переключить на них.

**Отклонено.**
- *Серверная фильтрация с самого начала* — `data.issues` уже загружены,
  клиентская проще и мгновеннее; серверную включим, если проектам перевалит за
  200 задач.
- *Оставить `Backlog.tsx` с одной секцией «все задачи»* — половинчато; отдельный
  экран «Список задач» с фильтрами — то, что просит SCOPE.

### D6. Гигиена из финального аудита — Фазой 0, отдельным PR

**Рекомендация.** До активной правки клиента:
- **`client`-job в `.github/workflows/test.yml`**: `checkout` → `setup-node` →
  `npm ci` (корень) → `npm run typecheck` → `npm run build`. Без БД, дёшево.
- **`contract.ts` `label()`** → `z.string().min(1).max(LIMITS.label.max)...`.
- **Чистка корневого `package.json`**: `npm uninstall` неиспользуемых
  (`@dnd-kit/*`, `@supabase/supabase-js`, `canvas-confetti` + `@types`, `date-fns`,
  `framer-motion`, `lucide-react`, `react-router-dom`, `recharts`, `uuid` +
  `@types/uuid`) — **проверить `grep` по `src/` заново** перед удалением каждой.
- **`package.json` (корень)** `name` → `taskira` (или `taskira-client`).
- **`ldapjs` — НЕ трогаем** в этом PR. Гигиена нулевого риска (архивный пакет,
  только в моке LDAP), но тематически не связана с UI-реструктуризацией — не
  смешивать в тот же PR; отдельный тикет на будущее.

**Обоснование.** Аудит трижды подряд фиксировал эти пункты; клиентский CI —
прямая страховка для дизайн-фазы (правки клиента участятся). Один маленький
безопасный PR.

**Отклонено.** Тащить гигиену внутрь фаз реструктуризации — размывает дифы;
отдельным PR она проходит ревью за минуту.

### D7. Порядок и ветвление

**Рекомендация.** Фаза 0 — **отдельный PR** (гигиена + CI), влить первой. Далее
одна ветка `feat/ui-restructure`, фазы 1→5 отдельными коммитами, один PR в конце.
Фаза 6 (верификация/доки) — в том же PR.

**Обоснование.** Фаза 0 не зависит от остального и снижает риск; реструктуризация
логически связная (общий «убрать наследие Jira»), дробить на 5 PR — оверхед.

### D8. Что НЕ входит в этот заход

- Визуальный редизайн (цвета, отступы, типографика, компоновка) — отдельно.
- Story points → «сложность» (SCOPE) — отдельный заход (не в задании).
- Переименование `Issue.epicId` → `directionId` в контракте/типах.
- Переименование `ViewId` `backlog` → `list`, `timeline` → …; смена kbd-раскладки.
- Полный переписывание `DocsView` (localStorage-тексты, «Сбросить демо»).
- Убрать/упростить `TimelineView` (SCOPE допускает удаление) — здесь только
  переименование терминов.
- WebSocket, `GET /issues/:id/activity` и прочие follow-up'ы прошлых миграций.

---

## 4. План по фазам

### Фаза 0 — Клиентский CI + гигиена  *(отдельный PR, первым)*

- `.github/workflows/test.yml` — job `client` (typecheck + build корня, без БД).
- `contract.ts` `label()` — `.min(1)`.
- Корневой `package.json` — `npm uninstall` неиспользуемых (после `grep`-проверки
  каждой), `name` → `taskira`.
- Проверка: `npm run typecheck` (корень + `server`), `npm run build`, `npm test`
  (сервер) — всё зелёное; новый job `client` — зелёный на самом PR.

### Фаза 1 — Убрать спринты

- **`server/migrations/012_drop_sprints.sql`** — `ALTER TABLE issues DROP COLUMN
  sprint_id`; `DROP TABLE sprints`. Комментарий-шапка (как 006/009): необратима,
  снять дамп при необходимости.
- **Сервер** — удалить `routes/sprints.ts`, `services/sprints.ts`, регистрацию;
  вычистить `routes/issues.ts` (`PATCH /:id/sprint`, ветка `sprintId`,
  `IssueQuery.sprint`, `INSERT`/`mapIssue`), `routes/projects.ts` (bootstrap
  `sprints`), `contract.ts` (`SPRINT_STATUSES`, `MoveToSprintBody`, `sprintId` в
  Create/Patch, `sprint` в Query, `WsMessage.sprint:changed`),
  `services/issues.ts` (`sprint_id`/`sprintId`), `seedProject.ts` (вставка),
  `permissions.ts` (`manageSprints` из `PermId`/`MATRIX`/`PERM_NAMES`).
- **Клиент** — `types.ts` (`Sprint`, `Data.sprints`, `Issue.sprintId`),
  `src/api/index.ts` (`sprintsApi`, `issuesApi.setSprint`), `src/permissions.ts`
  (`manageSprints`), `store.tsx` (`data.sprints`, `setSprint`/`startSprint`/
  `completeSprint`, `mapIssue`), `Board.tsx` (снять фильтр по спринту + баннер),
  `IssueModal.tsx` / `CreateIssueModal.tsx` (поле «Спринт»). `Backlog.tsx` —
  временно оставить компилируемым (полная переработка — Фаза 5); проще сразу
  сделать Фазу 5 следом.
- **Тесты** — из `server/test/*` убрать кейсы про `manageSprints` / `sprintId` /
  `/sprints` (роли, multiproject); фикстура `helpers.ts` — `p1status` оставить,
  сид-спринт убрать; `resetDb` — `sprints` из `TRUNCATE`.
- Проверка: `typecheck` 0 (сервер + клиент), `npm test` — прежние минус
  спринт-кейсы, зелёные; `npm run build`.

### Фаза 2 — «Эпик» → «Направление»

- Замена подписей/текстов в `IssueModal`, `CreateIssueModal`, `Board`, `Backlog`
  (после Ф5), `TimelineView`, `DocsView` (только термин + снятие абзацев про
  спринты). Внутренние `epicId`/`epic_id` — без изменений.
- Проверка: `typecheck`/`build`; скриншоты карточки задачи, таймлайна.

### Фаза 3 — Точки создания задачи

- `Board.tsx` — кнопка «+»/`<QuickCreate>` только у первого `category==='todo'`
  столбца (по `position`); у остальных убрать.
- Проверка: `typecheck`/`build`; скриншот доски (одна «+», в первой колонке).

### Фаза 4 — Главный экран

- **Сервер** — `server/src/routes/home.ts` *(или в существующий project-less
  блок)*: `GET /api/issues/assigned-to-me`. Тест `server/test/home.test.ts`:
  отдаёт только мои открытые задачи по видимым проектам; чужие/закрытые/из
  недоступных проектов — нет.
- **Клиент** — `src/api/index.ts` `issuesApi.assignedToMe()`; типы; `store.tsx`
  `bootStatus += "home"`, правка `bootstrap()` (≥2 проекта → home; 1 → как
  сейчас; прямая ссылка `#/issue/...` — минуя home), экшен «на главный»
  (`goHome()`). Поведение при смене числа проектов + одноразовый тост
  (`localStorage taskira.homeIntro`) — по **D4-transition** в §3.
  `src/components/HomeView.tsx` *(новый)* — шапка + «Мои задачи» + «Недавние
  проекты». `App.tsx` — `bootStatus==="home" → <HomeView>`. Топбарная крошка
  «Проекты» — кликабельна (`goHome`).
- Проверка: `typecheck`/`build`; **скриншоты** (`scripts/shot.mjs`): главный
  экран с задачами и проектами, вход в проект по клику, возврат на главный.
- Живой прогон: пользователь с 2+ проектами и назначенными задачами.

### Фаза 5 — «Бэклог» → «Список задач»

- `Sidebar.tsx` — label «Список задач».
- `Backlog.tsx` — переписать: плоский список + панель **фильтров**
  (статус / исполнитель / тип / текст / просроченные) **и сортировки**
  (приоритет / срок / обновление / ключ, asc-desc); контекстное меню без
  спринт-пунктов.
- Проверка: `typecheck`/`build`; **скриншоты**: список по умолчанию, с
  применённым фильтром, со сменой сортировки.

### Фаза 6 — Верификация

- `npm test` (сервер) + новые `home.test.ts`; `typecheck` 0 (обе стороны);
  `npm run build`.
- **Ручной чек-лист** — блок «UI-реструктуризация» в `server/README.md`
  (миграция 012; нет `/api/sprints`; `manageSprints` нет в матрице; главный
  экран; «Список задач»; «Направление»).
- **`ARCHITECTURE.md`** — п. 6 «Порядка разработки» и «Что убирается» → отметить
  сделанное (спринты убраны, «Направление», «Список задач»).
- **`SCOPE.md`** — таблица терминологии: «Epic → Направление» и «Backlog →
  Список задач» помечены реализованными.
- Скриншоты всех затронутых экранов (`scripts/shot.mjs`).

### Follow-ups (не в этом заходе)

- Story points → «сложность» (3 значения).
- Переименование `epicId`/`ViewId`/kbd, полный `DocsView`.
- Убрать/упростить `TimelineView`.
- Серверная фильтрация «Списка задач» при росте объёма (сортировка и фильтры
  MVP — клиентские, в этом заходе).
- `assigned-to-me` с пагинацией по проектам.
- `ldapjs` (devDep, архивный) — заменить/убрать отдельным тикетом.

---

## 5. Риски и внимание

- **Миграция 012 необратима** — удаляет `sprints` + `issues.sprint_id`. На dev-БД
  там только дефолтный seed-спринт (реального планирования нет) → дамп не нужен,
  дропаем. На инсталляции с реальным планированием в спринтах — снять дамп
  (`pg_dump -t sprints`) до применения; в README-чек-лист явный пункт. Историю в
  `audit_log` не трогаем.
- **`MATRIX` меняется в ОБЕИХ копиях `permissions.ts` одним коммитом** (правило
  CLAUDE.md). Удаление ключа `manageSprints` — все вызовы (`roleHas`/`requirePerm`/
  `can`/`PERM_NAMES`/`PERMISSIONS`) снять синхронно, иначе не компилируется.
- **`PermissionsView`** (матрица прав на клиенте) — после удаления строка
  «Управление спринтами» исчезает; проверить, что таблица рендерится (тотальность
  `permMeta()`).
- **`Backlog.tsx`** — если Фазу 1 влить без Фазы 5, экран временно сломан
  (`data.sprints` нет). Делать Ф5 сразу за Ф1 в одной ветке, не мержить
  промежуточно.
- **Хэш-навигация к задаче** (`#/issue/<pid>/<iid>`) — главный экран и «Мои
  задачи» переиспользуют существующий механизм (`readIssueHash` + эффект в
  `store.tsx`); проверить кросс-проектный переход (`switchProject` затем открыть).
- **`bootstrap()` — горячий путь входа.** Добавление ветки «home» не должно
  ломать: 1 проект (сразу внутрь), 0 проектов (solo / пусто), прямую ссылку
  `#/issue/...` (должна вести в нужный проект, минуя/через home).
- **`assigned-to-me`** — предикат видимости проекта обязан совпасть с
  `listVisibleProjects` (участник ∪ департамент ∪ `is_shared` ∪ admin), иначе
  экран покажет задачи из проектов, которые пользователь потом не сможет открыть.
- **Клиентский CI (Фаза 0)** — после включения `client`-job красный клиентский
  `tsc` начнёт блокировать мерж; убедиться, что `main` сейчас зелёный (по
  аудиту — да, 0 ошибок).
- **`WsMessage.sprint:changed`** — тип не используется (Этап 3c), но удалить
  аккуратно (единый `WsMessage` union).
- **Скриншоты** делаются на dev-данных; на чистой БД (1 сид-проект) главный
  экран не покажется (правило «≥2 проекта») — для скриншота Фазы 4 завести
  второй проект/отдел временно.

---

## 6. Оценка объёма

| Фаза | Область | Размер |
|---|---|---|
| 0 | клиентский CI-job + `label().min(1)` + чистка зависимостей + `name` | S |
| 1 | убрать спринты: миграция 012 + сервер (роуты/сервис/контракт/MATRIX×2/seed/DTO) + клиент (типы/api/store/Board/IssueModal/CreateIssueModal) + тесты | **L** |
| 2 | «эпик» → «направление» (подписи/тексты, ~6 файлов) | S |
| 3 | «+» только в первой колонке доски | XS |
| 4 | главный экран: `GET /issues/assigned-to-me` + тест; `<HomeView>` + правка `bootstrap` + `App.tsx` | **M–L** |
| 5 | `Backlog.tsx` → «Список задач» (плоский список + фильтры + сортировка) | **M** |
| 6 | верификация + чек-лист + `ARCHITECTURE`/`SCOPE` + скриншоты | S–M |

Критический путь: 0 (отдельный PR) → 1 → 5 (вместе) → 2 → 3 → 4 → 6.
Фаза 3 — почти тривиальна. Фазы 2/3 можно делать в любой момент после 1.

---

## 7. Открытые вопросы — ЗАКРЫТЫ

Все подтверждены (см. §3 «РЕШЕНО»):

1–2. **D1** — спринты удаляем полностью (не tombstone), `manageSprints` из
   `MATRIX` ×2, `issues.sprint_id` дропаем; миграция 012 необратима. Дамп
   `sprints` на dev не нужен (только seed-спринт), просто дроп.
3. **D2** — «Направление» только как отображаемый термин; `epicId`/`epic_id` в
   коде и API не переименовываем.
4. **D3** — «+» на доске у первого `todo`-столбца (по `position`), у остальных
   убрать; «+ Создать» в шапке без изменений.
5. **D4** — главный экран при **≥ 2** проектах; при 1 — сразу в проект. Поведение
   при переходе числа проектов (1↔≥2) + одноразовый тост — прописано явно в §3
   (D4-transition), не как побочный эффект.
6. **D4** — `GET /api/issues/assigned-to-me` (project-less, открытые задачи по
   видимым проектам) — принято.
7. **D4** — возврат на главный кликом по крошке «Проекты» в топбаре
   (`<HomeView>` поверх, проект не выгружаем).
8. **D5** — «Список задач» с **клиентской** фильтрацией **и сортировкой** (задачи
   уже загружены, лимит 200); `ViewId` id `backlog` и kbd `2` — внутренние.
9. **D5** — фильтры: статус, исполнитель, тип, текст, «просроченные».
   **Сортировка добавляется сразу** (приоритет — дефолт / срок / обновление /
   ключ, asc-desc), не в follow-up.
10. **D6/порядок** — Фаза 0 отдельным PR первой; фазы 1–6 одной веткой
    `feat/ui-restructure`, один PR. Документ `UI_RESTRUCTURE.md`.
11. **`ldapjs`** — **не трогаем** в этом заходе; отдельный тикет.
